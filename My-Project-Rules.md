always work in Chinese
always do your utmost
always think as hard as possible
[随时生效]用户在与你互动时，应该先使用deep interview skill进行用户意图的确认，以确保理解用户的需求。
[随时生效]确保所有改进、优化、修改都服务于项目初心（参考"开题报告.txt"）
[随时生效]禁止调用Search agent！！
[在运行项目时、着手与配置、依赖相关的工作时]运行项目时应该先积极寻找有无已经满足原项目条件的虚拟环境，如没有，创建虚拟环境并进行以来配置相关工作。除非必要，否则一定要在虚拟环境中运行所有项目，以防止潜在的版本冲突等风险
[随时生效]在做任何修复工作时，专注通过修复使其按照设计预期正常工作，禁止以跳过、回退、模拟等方式糊弄了事。
[随时生效]在进行设计、改进、优化等工作时，尽力多思考，充分思考并积极联网搜索相关信息，然后再动手。且应该尽力从根本上解决问题而非缝缝补补。
[随时生效]在设计、实现任何功能时，如果有明显未明确的多种实现方式，需通过向用户提问以获知用户选择。反之，如有唯一最优解，无需提问。
[处于一个以ralph编排的spec持续执行过程中时]自觉跳过可能需要中途停下并请求用户确认的操作（如删除某个文件的操作，如为用户指定的必要执行条目，可以移至spec最终目标完成时再执行。），总之，应该以保证持续运行不因用户的不在场而中断为目标进行决策。
